# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: zgsp26.conf.request.confrequestapp
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://s40lp1.ucc.cit.tum.de
          client: '324'
        app:
          name: ZCONF_REQ_UI
          description: Configuration Change Request Fiori Application
          package: ZGSP26SAP06
          transport: S40K908611
        exclude:
          - /test/
